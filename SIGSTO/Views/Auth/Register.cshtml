@{
    ViewData["Title"] = "Inscription";
    Layout = "_Layout";
}

<div class="auth-container">
    <div class="auth-card">
        <h1>Inscription</h1>
        <p class="subtitle">Creez votre compte etudiant</p>

        @if (ViewBag.Error != null)
        {
            <div class="alert alert-danger">@ViewBag.Error</div>
        }

        <form method="post">
            <div class="form-group">
                <label>Nom</label>
                <input type="text" name="nom" class="form-control" required />
            </div>
            <div class="form-group">
                <label>Prenom</label>
                <input type="text" name="prenom" class="form-control" required />
            </div>
            <div class="form-group">
                <label>Email institutionnel</label>
                <input type="email" name="email" class="form-control" placeholder="prenom.nom@@etu.uae.ac.ma" pattern=".+@@etu\.uae\.ac\.ma$" required />
            </div>
            <div class="form-group">
                <label>Mot de passe</label>
                <input type="password" name="password" class="form-control" minlength="6" required />
            </div>
            <div class="form-group">
                <label>Confirmer le mot de passe</label>
                <input type="password" name="confirmPassword" class="form-control" minlength="6" required />
            </div>
            <button type="submit" class="btn btn-primary">S'inscrire</button>
        </form>

        <div class="links">
            <p>Deja inscrit ? <a href="/Auth/Login">Se connecter</a></p>
        </div>
    </div>
</div>
